import { styled } from 'styled-components';
import { height } from '../../constants/header';

const Main = styled('main')`
  padding-top: ${height};
`;

const Styled = { Main };

export default Styled;
